pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh "git clone https://github.com/saleemshaik-wq/git_jenkins_repo"
            }
        }
    }
}
